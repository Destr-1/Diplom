program optim;
{ Оптимизация работы турбогенераторов: Минимизировать функцию G=G1+G2+G3+G4
  где Gi=Gi(Ni,Qni)
  с ограничениями:    N1+N2+N3+N4=N
                      Qn1+Qn2+Qn3+Qn4=Qn
                      Nj min<=Nj<=Nj max, j=1,2,...,M
                      0<=Qnj<=Qnj max
                      Sн(Nj)<=Gj<=Sв(Nj), j=1,2,...,M
  Минимизация будет производится методом штрафных функций
    ( Модифицированным методом Лагранжа [Моисеев "Методы оптимизации" стр 248]
}
  const m=6; {число функций - ограничений}
        n=2; {размерность задачи}

  type  mar=array [1..m] of real;{тип для массивов
                                     связанных с числом функций - ограничений}

        nar=array [1..n] of real; {тип для массивов связанных
                                       с размерностью задачи}

        functype=function(t:real):real;{функциональный тип для
        передачи функции в качестве параметра при вычислении производной}
  var   fi:mar;
{$I Test} {  Вычисление основной минимизирующей функции бикубическими сплайнами}
{$I FI}   {Функции - ограничения}
{$I Lagr} {Минимизация методом штрафных функций}
begin
  writeln;
  Lagr;
end.